<h1>Test</h1>

<div class="row">
  {{#each model as |blog|}}
    {{#if blog.dailyStory}}
      {{featured-entry blog=blog}}
    {{/if}}
  {{/each}}
  {{#each model as |blog|}}
    {{#unless blog.dailyStory}}
      {{blog-entry blog=blog}}
    {{/unless}}
  {{/each}}
</div>
